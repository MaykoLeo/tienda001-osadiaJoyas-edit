# Este archivo contiene solo valores placeholder NO sensibles.
# Las variables reales deben configurarse en .env.local (local) o en el dashboard de Vercel (producción).
# NUNCA pongas credenciales reales aquí.

NEXT_PUBLIC_SITE_URL=http://localhost:9002
